import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function OfflineDesignacoes() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Designações Offline</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Modo offline - implementação pendente</p>
      </CardContent>
    </Card>
  );
}