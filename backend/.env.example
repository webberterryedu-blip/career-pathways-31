# =====================================================
# Sistema Ministerial - Backend (.env.example)
# =====================================================
# Copie para backend/.env e preencha com valores reais.
# NÃO versione backend/.env. Use Secrets no CI/CD.
# =====================================================

# Ambiente e porta
NODE_ENV=development
PORT=3001

# CORS: origem do frontend em dev
CORS_ORIGIN=http://localhost:8080

# Supabase (backend usa SERVICE ROLE KEY para operações administrativas)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key-here

# Opcional: token pessoal para MCP local (não necessário em produção)
# SUPABASE_ACCESS_TOKEN=sbp_your-personal-access-token

# Opcional: conexão direta ao Postgres (se usada por scripts)
# DATABASE_URL=postgresql://postgres:password@db.your-project-id.supabase.co:5432/postgres

# Observação:
# - Garanta que a pasta docs/Oficial exista e tenha permissão de escrita.
# - Ajuste PORT/CORS conforme necessário.

